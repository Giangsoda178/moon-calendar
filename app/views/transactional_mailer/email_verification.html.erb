<h2 style="margin: 0 0 16px 0; color: <%= email_colors[:foreground] %>; font-size: 24px; font-weight: 600;">
  Verify Your Email Address
</h2>

<p style="margin: 0 0 16px 0;">
  Hi <%= @user.respond_to?(:name) ? @user.name : "there" %>,
</p>

<p style="margin: 0 0 16px 0;">
  Thanks for signing up! Please verify your email address to complete your registration:
</p>

<%= render "mailers/shared/button", text: "Verify Email", url: @verification_url, icon: :check %>

<p style="margin: 16px 0 0 0; font-size: 14px; color: <%= email_colors[:muted_foreground] %>;">
  If you didn't create an account, no action is needed.
</p>

<%= render "mailers/shared/divider" %>

<p style="margin: 0; font-size: 12px; color: <%= email_colors[:muted_foreground] %>;">
  If the button doesn't work, copy and paste this URL into your browser:<br>
  <a href="<%= @verification_url %>" style="color: <%= email_colors[:primary] %>; word-break: break-all;">
    <%= @verification_url %>
  </a>
</p>
